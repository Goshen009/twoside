import { HandCoins } from "lucide-react";

export function LoansPage() {
  return (
    <div className="min-h-dvh flex flex-col items-center justify-center gap-2 px-4 pt-4 pb-24">
      <HandCoins className="h-5 w-5 text-muted" />
      <p className="text-xs text-muted">Loans tracking lands in a later bite.</p>
    </div>
  );
}
